# ==============================================================================
# Grid Tile Layout (Mixin)
# ==============================================================================
# Location: ~/.config/aerofi/themes/layouts/grid.toml
# Import via: imports = ["layouts/grid.toml"]
# ==============================================================================

[font]
family = "SF Pro Text"
size = 12.5
fallback = ["SF Pro", "Helvetica Neue", "Arial"]

[window]
width = 560.0
height = 380.0
padding = 12.0
background = "$bg"
blur = true
background_opacity = 0.95
corner_radius = 16.0
border_width = 1.0
border_color = "$border"

[mainbox]
orientation = "vertical"
children = ["InputBar", "ListView"]

[inputbar]
height = 40.0
padding = [8.0, 12.0]
margin = [0.0, 0.0, 8.0, 0.0]
background = "$surface"
text_color = "$text"
placeholder = "Search apps & scripts…"
placeholder_color = "$subtle"
corner_radius = 10.0
icon = "❯"
icon_color = "$accent"

[listview]
columns = 4
spacing = 8.0
scrollbar = false
empty_text = "No matching results"
empty_text_color = "$subtle"

[listview.category_badge]
show = false
color = "$subtle"

[listview.alias_badge]
show = true
color = "$accent"

[element]
padding = [8.0, 10.0]
corner_radius = 10.0
background = "$surface"
text_color = "$text"
description_color = "$subtle"
show_icons = true
icon_size = 32.0
layout = ["icon", "name"]

[element.selected]
background = "$surface2"
text_color = "$text"
description_color = "$accent"

[element.hover]
background = "$surface2"
text_color = "$text"
description_color = "$subtle"
